(() => {
  'use strict';
  /* Vehicle mode removed by design: Hiro uses a pure procedural 3D environment.
     No external 3D model, no duplicate Three.js instance, no forced scrolling. */
  document.documentElement.classList.add('hiro-vehicle-disabled');
})();

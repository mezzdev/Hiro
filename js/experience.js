(() => {
  'use strict';
  /* Compatibility layer: the 3D engine now lives in main.js.
     This file intentionally contains no section transitions and no models. */
  document.documentElement.classList.add('hiro-experience-ready');
})();
